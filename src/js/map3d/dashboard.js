class DashBoard{
    constructor(options){
        update(options);
    }
    update(options){
        this.options = Object.assign({},options);
        this.create();
    }
    create(){
        
    }
}